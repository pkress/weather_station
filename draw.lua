-- the draw() routine
function draw()
    disp:drawStr(0, 0*lineH, dstr)
    disp:drawStr(0, 1*lineH, tstr)
    disp:drawStr(0, 2*lineH, wstr)
    disp:drawStr(0, 3*lineH, IPstr1)
    disp:drawStr(0, 4*lineH, IPstr2)
end

